<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />

    <!--Colocado pra tentar chat-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.Geochat"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="chaveaqui" />
        <activity
            android:name=".MainScreen"
            android:exported="false"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/Theme.Geochat" />
        <activity
            android:name=".models.BottomNavItem"
            android:exported="false"
            android:label="@string/title_activity_bottom_nav_graph"
            android:theme="@style/Theme.Geochat" />
        <activity
            android:name=".utils.Constants"
            android:exported="false"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.Geochat" />
        <!--
        <activity
            android:name=".BottomBarScreen"
            android:exported="false"
            android:label="@string/title_activity_main_nav_bar"
            android:theme="@style/Theme.Geochat" />
        <activity
            android:name=".ChatMenu"
            android:exported="false"
            android:label="@string/title_activity_chat_menu"
            android:theme="@style/Theme.Geochat" />
        <activity
            android:name=".screens.Settings"
            android:exported="false"
            android:label="@string/title_activity_config_menu"
            android:theme="@style/Theme.Geochat" />
            -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Geochat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>